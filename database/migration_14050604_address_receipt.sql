-- ============================================================
-- مایگریشن افزایشی ۱۴۰۵/۰۶/۰۴
-- ۱) فیلد آدرس پیش‌فرض برای پروژه‌ها (پر شدن خودکار آدرس در ثبت مأموریت)
-- ۲) فیلدهای تصویر رسید رفت و برگشت برای مأموریت‌ها (الزامی برای اسنپ/تپسی و آژانس)
-- این اسکریپت روی دیتابیس‌های موجود اجرا می‌شود (ALTER TABLE).
-- ============================================================

ALTER TABLE projects ADD COLUMN address TEXT;
ALTER TABLE missions ADD COLUMN outbound_receipt TEXT;
ALTER TABLE missions ADD COLUMN inbound_receipt TEXT;
